spring:
  application:
    name: jc-club-practice
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: http://192.168.174.100:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
        shared-configs: # 共享配置
          - data-id: jc-club-practice.yaml
            refresh: true
      discovery:
        enabled: true
        server-addr: http://192.168.174.100:8848