spring:
  application:
    name: jc-club-subject
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: http://192.168.174.100:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: http://192.168.174.100:8848